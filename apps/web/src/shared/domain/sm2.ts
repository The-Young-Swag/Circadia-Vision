/**
 * Domain — SM-2 pure logic (Guide §15, §57)
 * Single source of truth for spaced-repetition. No React, no DB, no HTTP.
 * UI → Service → Domain. Testable: Input → Expected Output.
 */
export * from '#/shared/lib/sm2'
